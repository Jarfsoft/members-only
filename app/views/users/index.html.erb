<div class="float-right">

</div>

<h1>Members Only Users</h1>

<% if signed_in? %>
<% @user.each do |user| %>
  <% if signed_in? %>
    <p class="float-right">
      User:
      <%= user.email %>
    </p>
    <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>

<% end %>
<% end %>


<%= link_to 'Sign in', new_user_session_url %>
<%= link_to "Sign Up", new_user_path %>
